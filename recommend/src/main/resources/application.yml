server:
  port: 1003

spring:
  cloud:
    loadbalancer:
      retry:
        enabled: true
  application:
    name: usercenterservice

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:1000/eureka/